services:
  postgres:
    image: postgres:15-alpine
    container_name: shareit-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shareit
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  server:
    build: server
    container_name: shareit-server
    ports:
      - "9090:9090" # Внутренний порт сервера
    environment:
      - SERVER_PORT=9090
      - SPRING_DATASOURCE_URL=jdbc:postgresql://shareit-postgres:5432/shareit
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
    depends_on:
      postgres:
        condition: service_healthy

  gateway:
    build: gateway
    container_name: shareit-gateway
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - SHAREIT-SERVER_URL=http://shareit-server:9090
    depends_on:
      - server

volumes:
  postgres_data: